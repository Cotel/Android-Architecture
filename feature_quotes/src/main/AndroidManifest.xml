<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.cotel.architecture.quotes">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_feature_quotes">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity android:name=".presentation.QuoteListActivity" />
    </application>
</manifest>

